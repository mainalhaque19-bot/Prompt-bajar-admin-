import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.promptbajar.admin',
  appName: 'Prompt Bajar Admin',
  webDir: 'dist'
};

export default config;
